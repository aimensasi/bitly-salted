<ul class="list-group">
	<% @urls.each do |url| %>
			<li class="lits-group-item">
				<h2 class="link"><%= url.long_url %></h2>	
				<h4 class="sub-link"><%= url.short_url %></h4>
			</li>
	<%end%>
</ul>